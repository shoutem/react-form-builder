const key = 'shoutem.builder.components.color-picker';

const UNDEFINED_COLOR = `${key}.undefined-color`;
const INVALID_COLOR = `${key}.invalid-color`;

export default {
  UNDEFINED_COLOR,
  INVALID_COLOR,
};
